@model IEnumerable<EsportsTour.Models.Jeux>

@{
    ViewData["Title"] = "Index";
}


@if (User.IsInRole("Admin"))
{
   
<section id="shop-cart-section" class="pt-120 pb-120">
    <div class="overlay">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <!-- Change from col-lg-8 to col-lg-12 -->
                    <h1>Liste des jeux</h1>
                    <a asp-action="Create" class="cmn-btn"> Create New</a>

                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.NomJeu)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Categorie)
                                    </th>
                                    <th>
                                        ACTION
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th scope="row">
                                                <img src="~/img/@Html.DisplayFor(modelItem => item.ImgJeu)" alt="image" height="80" width="80">
                                            <span> @Html.DisplayFor(modelItem => item.NomJeu)</span>
                                        </th>
                                        <td> @Html.DisplayFor(modelItem => item.Categorie)</td>
                                        <td>
                                          
                                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                                            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
}
else{
    <br />
    <br />
    <br />
    <br />
    <section id="available-game-section">
        <div class="overlay pb-120">
            <div class="container wow fadeInUp">
                <div class="main-container">
                    <div class="row justify-content-between">
                        <div class="col-lg-10">
                            <div class="section-header">
                                <h2 class="title">Les jeux disponible</h2>
                                <p>Tous les différent jeux disponible aujourd'hui!</p>
                            </div>
                        </div>
                    </div>
                    <div class="available-game-carousel">
                        @foreach (var item in Model)
                        {
                        <div class="single-item">
                                <a href="#"> <img src="~/img/@Html.DisplayFor(modelItem => item.ImgJeu)" alt="image" height="345" width="259"></a>
                        </div>
                        }
                    </div>
                    <div class="btn-area text-center">
                        <a href="tournaments.html" class="cmn-btn">Voir tous</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
}